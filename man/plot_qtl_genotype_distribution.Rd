% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_falconer_distribution.R
\name{plot_qtl_genotype_distribution}
\alias{plot_qtl_genotype_distribution}
\title{Plot Falconer Genotype-Specific Quantitative-Trait Distributions}
\usage{
plot_qtl_genotype_distribution(
  qtl_var,
  tau,
  pd,
  type = c("density", "histogram"),
  scale = c("density", "frequency"),
  n = 3000,
  seed = NULL,
  show_means = TRUE,
  verbose = FALSE,
  title = NULL,
  return_data = FALSE
)
}
\arguments{
\item{qtl_var}{QTL variance in \verb{(0, 1)}.}

\item{tau}{Dominance-to-additivity ratio.}

\item{pd}{Increaser-allele frequency in \verb{(0, 1)}.}

\item{type}{Either \code{"density"} for theoretical normal curves or
\code{"histogram"} for a simulated population.}

\item{scale}{Either \code{"density"} or \code{"frequency"}. For theoretical curves,
frequency scaling weights each density by its Hardy--Weinberg genotype
frequency. For histograms, it selects normalized densities or counts.}

\item{n}{Total population size simulated in histogram mode.}

\item{seed}{Optional non-negative integer used locally for reproducible
histogram simulation. The caller's random-number state is restored.}

\item{show_means}{Logical; mark the three theoretical genotype means.}

\item{verbose}{Logical; passed to the validated Falconer parameter backend.}

\item{title}{Optional plot title.}

\item{return_data}{Logical; return the plotting data frame instead of the
ggplot object. The Falconer model is retained in its \code{falconer_model}
attribute.}
}
\value{
A \code{ggplot} object, or a plotting data frame when
\code{return_data = TRUE}.
}
\description{
Displays the three normal quantitative-trait distributions implied by a
validated single-trait Falconer model. Large QTL variance separates the
genotype distributions relative to their residual variation; small QTL
variance makes them overlap heavily.
}
\details{
Conditional on genotype \code{j}, the plotted trait follows a normal
distribution with the corresponding Falconer mean and common residual
variance \code{1 - qtl_var}. Genotypes 0, 1, and 2 have Hardy--Weinberg
frequencies \code{(1 - pd)^2}, \code{2 * pd * (1 - pd)}, and \code{pd^2}. Genotypes may
also be interpreted as \code{bb}, \code{Bb}, and \code{BB}, respectively.

Density mode is analytic and performs no simulation. Histogram mode first
samples genotypes from their Hardy--Weinberg frequencies and then samples
trait values from the corresponding conditional normal distributions.
The x-axis is the quantitative-trait value. In analytic density mode, the
y-axis is genotype-specific density when \code{scale = "density"} and
population-weighted density when \code{scale = "frequency"}. In histogram mode,
the y-axis is normalized density or bin count for those respective scales.
}
\examples{
# A large QTL effect visibly separates the genotype distributions.
plot_qtl_genotype_distribution(
  qtl_var = 0.5, tau = 0, pd = 0.25, scale = "frequency"
)

# A very small QTL effect produces almost complete overlap.
plot_qtl_genotype_distribution(
  qtl_var = 0.0005, tau = 0, pd = 0.25, scale = "frequency"
)

}
\seealso{
\code{\link[=qtl_falconer_parameters]{qtl_falconer_parameters()}}, \code{\link[=plot_qtl_anova_power]{plot_qtl_anova_power()}}, and
\code{\link[=plot_qtl_threshold_chisq_power]{plot_qtl_threshold_chisq_power()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdt_functions.R
\name{tdt_plot_sample_size}
\alias{tdt_plot_sample_size}
\title{Required sample size sensitivity to misclassification and heterogeneity}
\usage{
tdt_plot_sample_size(
  pd,
  prev,
  R1,
  R2,
  alpha = 0.05,
  delta_prime = 1,
  target_power,
  misclass_seq = seq(0, 0.15, by = 0.01),
  heter_seq = seq(0, 0.5, by = 0.05),
  heter_fixed = 0,
  misclass_fixed = 0,
  title = "Required trios under misclassification and heterogeneity"
)
}
\arguments{
\item{pd}{Numeric in (0, 1). High–risk allele frequency at the marker.}

\item{prev}{Numeric in (0, 1). Disease prevalence \eqn{\phi_1}.}

\item{R1, R2}{Numeric. Genotype relative risks for the heterozygote and
homozygote, respectively.}

\item{alpha}{Numeric. Type I error rate for the TDT (default \code{0.05}).}

\item{delta_prime}{Numeric. Linkage disequilibrium scale parameter
\eqn{\delta'} (default \code{1}).}

\item{target_power}{Numeric in (0, 1). Desired power for the TDT.}

\item{misclass_seq}{Numeric vector. Sequence of phenotype
misclassification rates (pi01) to use on the x–axis of the
misclassification panel.}

\item{heter_seq}{Numeric vector. Sequence of heterogeneity rates
(proportion of trios not due to the locus, \eqn{1-\pi}) to use on the
x–axis of the heterogeneity panel.}

\item{heter_fixed}{Numeric scalar. Heterogeneity rate to hold fixed while
varying \code{misclass_seq} in the misclassification panel (typically \code{0}).}

\item{misclass_fixed}{Numeric scalar. Misclassification rate to hold fixed
while varying \code{heter_seq} in the heterogeneity panel (typically \code{0}).}
}
\value{
A \code{ggplot}/\code{patchwork} object showing two panels side–by–side:
required trios vs. misclassification and required trios vs. heterogeneity.
The function is mainly called for its side–effect of plotting.
}
\description{
Create a 1×2 panel plot showing how the required number of trios changes
as a function of phenotype misclassification and locus heterogeneity,
for a fixed target power.
}
\details{
This function is a graphical wrapper around \code{\link[=tdt_required_trios_full]{tdt_required_trios_full()}},
looping over user–supplied grids of error rates and extracting the
required sample size under misclassification and heterogeneity.

In the left panel, each value of \code{misclass_seq} is passed to
\code{\link[=tdt_required_trios_full]{tdt_required_trios_full()}} via the \code{misclass_rate} argument, with
\code{heter_rate = heter_fixed}. The right panel does the reverse, passing
\code{heter_seq} into \code{heter_rate} with \code{misclass_rate = misclass_fixed}.
}
\examples{
\donttest{
# Sample size sensitivity for power 0.80
tdt_plot_sample_size(
  target_power = 0.80,
  pd   = 0.30,
  prev = 0.05,
  R1   = 1.5,
  R2   = 2.25,
  alpha = 0.05,
  delta_prime = 1,
  misclass_seq  = seq(0, 0.15, by = 0.01),
  heter_seq     = seq(0, 0.50, by = 0.05),
  heter_fixed   = 0,
  misclass_fixed = 0
)
}
}
\seealso{
\code{\link[=tdt_required_trios_full]{tdt_required_trios_full()}}, \code{\link[=tdt_power_full]{tdt_power_full()}}
}

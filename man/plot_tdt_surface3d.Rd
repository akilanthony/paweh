% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tdt_surface3d.R
\name{plot_tdt_surface3d}
\alias{plot_tdt_surface3d}
\title{Plot a Generalized 3D TDT Sensitivity Surface}
\usage{
plot_tdt_surface3d(
  metric = c("power", "mssn"),
  scenario = c("misclassification", "heterogeneity", "no_error"),
  x = "pd",
  y = "misclass_rate",
  x_values = NULL,
  y_values = NULL,
  N = 600,
  target_power = 0.8,
  pd = 0.3,
  prev = 0.05,
  R1 = 1.5,
  R2 = 2.25,
  alpha = 0.05,
  delta_prime = 1,
  misclass_rate = 0.01,
  heter_rate = 0.1,
  ceiling_N = TRUE,
  title = NULL
)
}
\arguments{
\item{metric}{Character. Surface height: \code{"power"} (default) or \code{"mssn"}.}

\item{scenario}{Character. One of \code{"misclassification"} (default),
\code{"heterogeneity"}, or \code{"no_error"}.}

\item{x, y}{Character scalars naming two distinct swept parameters. Supported
axes are \code{"pd"}, \code{"prev"}, \code{"R1"}, \code{"R2"}, \code{"alpha"},
\code{"delta_prime"}, \code{"misclass_rate"}, and \code{"heter_rate"}.}

\item{x_values, y_values}{Numeric grid values for \code{x} and \code{y}. Each must
contain at least two distinct, finite values. If \code{NULL}, documented
parameter-specific defaults are used; see Details.}

\item{N}{Numeric scalar greater than zero. Number of affected trios, used
when \code{metric = "power"}.}

\item{target_power}{Numeric scalar in \verb{(0, 1)}, used when
\code{metric = "mssn"}.}

\item{pd}{Numeric in \verb{(0, 1)}. Fixed risk-allele frequency when \code{pd} is not
an axis.}

\item{prev}{Numeric in \verb{(0, 1)}. Fixed disease prevalence when \code{prev} is not
an axis.}

\item{R1, R2}{Positive numeric scalars. Fixed genotype relative risks when
they are not axes.}

\item{alpha}{Numeric in \verb{(0, 1)}. Fixed significance level when \code{alpha} is
not an axis.}

\item{delta_prime}{Numeric in \verb{[0, 1]}. Fixed positive linkage-disequilibrium
scale when \code{delta_prime} is not an axis.}

\item{misclass_rate}{Numeric in \verb{[0, 1)}. Fixed phenotype
misclassification rate when it is not an axis.}

\item{heter_rate}{Numeric in \verb{[0, 1)}. Fixed locus-heterogeneity rate when
it is not an axis.}

\item{ceiling_N}{Logical scalar. For MSSN surfaces, whether to plot the
integer ceiling of the canonical sample-size result (default \code{TRUE}). The
unrounded result remains available in the attached surface data.}

\item{title}{Optional character scalar. If \code{NULL}, an informative title is
generated from \code{metric} and \code{scenario}.}
}
\value{
An object inheriting from \code{"plotly"} and \code{"htmlwidget"}. The
Cartesian grid is attached as \code{attr(x, "surface_data")}, with dynamically
named axis columns, \code{metric_value} (the plotted height), and
\code{raw_metric_value}. The plotted z matrix is attached as
\code{attr(x, "surface_matrix")}, and calculation settings as
\code{attr(x, "surface_spec")}.
}
\description{
Builds an interactive sensitivity surface for model-based TDT power or
minimum sample size. Any two supported model parameters can be swept while
the remaining parameters are held fixed. Every grid point is evaluated by
the canonical \code{\link[=tdt_power]{tdt_power()}} or \code{\link[=tdt_mssn]{tdt_mssn()}} backend.
}
\details{
The supported axes and their default grids are:
\code{pd = seq(0.10, 0.50, length.out = 20)},
\code{prev = seq(0.01, 0.10, length.out = 20)},
\code{R1 = seq(1.1, 2, length.out = 20)},
\code{R2 = seq(1.2, 3, length.out = 20)},
\code{alpha = seq(0.01, 0.10, length.out = 20)},
\code{delta_prime = seq(0.25, 1, length.out = 20)},
\code{misclass_rate = seq(0, 0.20, length.out = 20)}, and
\code{heter_rate = seq(0, 0.50, length.out = 20)}.
The package's implemented LD convention is
\code{D = delta_prime * pd * (1 - pd)}, where \code{delta_prime} is the proportion of
maximum positive disequilibrium: \code{0} represents linkage equilibrium and \code{1}
represents maximum positive LD under the model assumptions. Negative LD
would require a different, allele-frequency-dependent normalization and is
not represented by this parameterization.

The surface is deliberately model-based. \code{N} and \code{target_power} define the
design calculation and therefore remain fixed rather than becoming axes.
The model-free inputs \code{ET}, \code{ENT}, and \code{n_trios}, as well as nonnumeric
controls such as \code{verbose}, are outside this visualization interface.
MSSN is measured in affected-child trios. \code{heter_rate} is the heterogeneous
fraction, \code{1 - pi}.

The canonical TDT backends report separate scenarios, not a combined-error
scenario. Consequently, \code{misclass_rate} is an active axis only for
\code{scenario = "misclassification"}, and \code{heter_rate} is active only for
\code{scenario = "heterogeneity"}. An inactive modifier axis is rejected rather
than silently producing a flat, misleading surface.

This function requires the optional \code{plotly} package. The plotting layer
introduces no independent statistical approximation: it only constructs the
grid, delegates calculations to \code{\link[=tdt_power]{tdt_power()}} or \code{\link[=tdt_mssn]{tdt_mssn()}}, extracts the
selected scenario, and renders the result.
}
\examples{
if (requireNamespace("plotly", quietly = TRUE)) {
  power_surface <- plot_tdt_surface3d(
    metric = "power", scenario = "misclassification",
    x = "pd", y = "misclass_rate",
    x_values = c(0.2, 0.3, 0.4),
    y_values = c(0, 0.03, 0.06),
    N = 600
  )

  mssn_surface <- plot_tdt_surface3d(
    metric = "mssn", scenario = "heterogeneity",
    x = "prev", y = "heter_rate",
    x_values = c(0.02, 0.05, 0.08),
    y_values = c(0, 0.1, 0.2),
    target_power = 0.8
  )
}

}
\seealso{
\code{\link[=tdt_power]{tdt_power()}}, \code{\link[=tdt_mssn]{tdt_mssn()}}, \code{\link[=plot_tdt_power]{plot_tdt_power()}},
\code{\link[=plot_tdt_mssn]{plot_tdt_mssn()}}
}

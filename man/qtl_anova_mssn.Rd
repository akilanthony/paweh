% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/falconer_quantitative.R
\name{qtl_anova_mssn}
\alias{qtl_anova_mssn}
\title{Minimum Sample Size for One-Way ANOVA Under the Falconer Model}
\usage{
qtl_anova_mssn(
  power,
  alpha,
  qtl_var,
  tau,
  pd,
  count_method = c("rounded", "expected"),
  multiple_of_three = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{power}{Target power in \eqn{(0,1)}.}

\item{alpha}{Significance level in \eqn{(0,1)}.}

\item{qtl_var}{Variance in the standardized trait attributable to the QTL.
Must be in \eqn{(0,1)}.}

\item{tau}{Finite dominance-to-additivity ratio \eqn{\delta/a}.}

\item{pd}{Frequency of the increaser allele. Must be in \eqn{(0,1)}.}

\item{count_method}{Genotype count method. \code{"rounded"} rounds expected
genotype counts to the nearest integer before computing the ANOVA
quantities. \code{"expected"} uses expected genotype counts directly
without rounding.}

\item{multiple_of_three}{Logical. If \code{TRUE}, searches sample sizes in
multiples of three. If \code{FALSE}, searches every integer sample size.}

\item{verbose}{Logical. If \code{TRUE}, prints a concise summary.}
}
\value{
An object of class \code{"qtl_anova_mssn"} containing the smallest
sufficient \code{N}, achieved power, degrees of freedom, non-centrality
parameter, genotype counts, and Falconer parameters.
}
\description{
Finds the smallest sufficient integer total sample size for the three-group
one-way ANOVA. Both denominator degrees of freedom and the non-centrality
parameter are recalculated for every candidate sample size.
}
\examples{
qtl_anova_mssn(
  power = 0.8, alpha = 0.0001, qtl_var = 0.025,
  tau = 0.5, pd = 0.15, verbose = FALSE
)

}

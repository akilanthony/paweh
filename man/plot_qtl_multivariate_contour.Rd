% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_falconer_multivariate.R
\name{plot_qtl_multivariate_contour}
\alias{plot_qtl_multivariate_contour}
\title{Plot a Two-Phenotype Falconer Mixture Surface}
\usage{
plot_qtl_multivariate_contour(
  qtl_var,
  tau,
  pd,
  cor_matrix,
  x_upper = NULL,
  x_lower = NULL,
  surface = c("density", "cdf"),
  show_thresholds = TRUE,
  show_means = TRUE,
  show_labels = TRUE,
  grid_n = 150L,
  title = NULL,
  return_data = FALSE
)
}
\arguments{
\item{qtl_var}{Numeric vector of two phenotype-specific QTL variances.}

\item{tau}{Numeric vector of two phenotype-specific dominance/additivity
ratios.}

\item{pd}{Shared increaser-allele frequency in \verb{(0, 1)}.}

\item{cor_matrix}{A positive-definite \verb{2 x 2} phenotype correlation matrix.}

\item{x_upper, x_lower}{Optional length-two vectors of upper- and lower-tail
selection percentages. Supply both or neither.}

\item{surface}{Either \code{"density"} or \code{"cdf"}.}

\item{show_thresholds}{Logical; display joint threshold regions when
thresholds are supplied.}

\item{show_means}{Logical; mark the three genotype-specific mean vectors.}

\item{show_labels}{Logical; label the affected and unaffected joint regions.}

\item{grid_n}{Integer number of grid points along each phenotype axis.}

\item{title}{Optional plot title.}

\item{return_data}{Logical; return the surface grid instead of the ggplot.
The validated model and threshold details are retained as attributes.}
}
\value{
A \code{ggplot} object, or the plotting grid when
\code{return_data = TRUE}. Returned data retain the validated model and
threshold details as attributes.
}
\description{
Visualizes the population mixture of three genotype-specific bivariate
normal distributions for exactly two quantitative phenotypes. It can show
either the mixture density or the mixture lower-tail CDF, together with the
joint threshold regions used by the Chapter 6.2 selected-sampling design.
}
\details{
In density mode, each grid value is the genotype-weighted mixture
\code{sum(pi[j] * f[j](y1, y2))}, where each \code{f[j]} is a bivariate-normal
density. In CDF mode, it is the lower-tail mixture probability
\verb{sum(pi[j] * P(Y1 <= y1, Y2 <= y2 | G = j))}. Density and CDF surfaces
therefore represent different mathematical quantities.

When thresholds are supplied, affected subjects occupy only the joint
upper-right region \verb{Y1 >= TU1 AND Y2 >= TU2}. Unaffected subjects occupy
only the joint lower-left region \verb{Y1 <= TL1 AND Y2 <= TL2}. Subjects in all
other regions are not selected. The statistical backend can support more
traits where documented; this conventional contour visualization is
intentionally restricted to two.
}
\examples{
cor_matrix <- matrix(c(1, 0.15, 0.15, 1), 2, byrow = TRUE)
plot_qtl_multivariate_contour(
  qtl_var = c(0.95, 0.92), tau = c(0, 0.5), pd = 0.5,
  cor_matrix = cor_matrix,
  x_upper = c(10, 10), x_lower = c(15, 15),
  surface = "cdf", grid_n = 30
)

}
\seealso{
\code{\link[=qtl_multivariate_power_full]{qtl_multivariate_power_full()}},
\code{\link[=qtl_multivariate_mssn_full]{qtl_multivariate_mssn_full()}}, and
\code{\link[=plot_qtl_multivariate_surface3d]{plot_qtl_multivariate_surface3d()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdt_functions.R
\name{plot_tdt_power_phenotype_misclassification}
\alias{plot_tdt_power_phenotype_misclassification}
\title{Plot TDT Power Sensitivity to Phenotype Misclassification}
\usage{
plot_tdt_power_phenotype_misclassification(
  pd,
  prev,
  R1,
  R2,
  alpha = 0.05,
  delta_prime = 1,
  N,
  misclass_seq = seq(0, 0.15, by = 0.01),
  heter_fixed = 0,
  title = "TDT power vs misclassification (pi01)"
)
}
\arguments{
\item{pd}{Numeric in (0,1). High-risk allele frequency at the marker.}

\item{prev}{Numeric in (0,1). Disease prevalence (phi1).}

\item{R1, R2}{Numeric. Genotype relative risks.}

\item{alpha}{Numeric. Significance level (default 0.05).}

\item{delta_prime}{Numeric. LD scale parameter (default 1).}

\item{N}{Integer. Number of affected trios.}

\item{misclass_seq}{Numeric vector. Sequence of misclassification rates.}

\item{heter_fixed}{Numeric. Heterogeneity rate held fixed (default 0).}

\item{title}{Character. Plot title.}
}
\value{
A \code{ggplot} object, returned invisibly after it is printed.
}
\description{
Plot TDT power vs phenotype misclassification rate (pi01),
holding heterogeneity fixed.
}
\details{
The x-axis is phenotype misclassification probability
\eqn{\pi_{01}} and the y-axis is the corresponding power from
\code{tdt_power()}. \code{N} is a fixed number of affected trios;
\code{heter_fixed} and all genetic-model parameters remain fixed while
\code{misclass_seq} is swept.
}
\examples{
# Power sensitivity to phenotype misclassification
plot_tdt_power_phenotype_misclassification(
  N    = 600,
  pd   = 0.30,
  prev = 0.05,
  R1   = 1.5,
  R2   = 2.25,
  misclass_seq = c(0, 0.03, 0.06),
  heter_fixed  = 0
)

}
\seealso{
\code{\link{tdt_power}},
\code{\link{plot_tdt_power_locus_heterogeneity}}, and
\code{\link{plot_tdt_power}}.
}
